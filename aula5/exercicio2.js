/*
1. Crie uma função chamada `calculaSoma` que aceite dois
 argumentos e imprima o resultado da soma entre eles.
2. Chame a função com dois números como argumentos.
*/

function calculaSoma(param1, param2) {
    console.log(param1 + param2);
  }

  calculaSoma(20,2);